<div id="body">

	<% @products.each do |product| %>


	<div class="float">
		<p><%= link_to image_tag(product.url, :size => "180x250"), "/products/#{product.id}" %></p><br />
			<p><%= link_to product.name, product_path(product) %></p>
	</div>
<% end %>
</div>








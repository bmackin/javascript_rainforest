


<% @products.each do |product| %>


<div class="float">
		<p><%= link_to image_tag(product.url), "/products/#{product.id}" %></p><br />
		<p><%= link_to product.name, product_path(product) %></p>
		</div>
<% end %>
</div>





